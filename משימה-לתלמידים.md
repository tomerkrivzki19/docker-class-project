# משימת דוקר, תקנו את הפרויקט והעלו אותו

## הסיפור

לפניכם פרויקט נוד שעובד מצוין. הקוד תקין, אל תיגעו בו.
הבעיה היא בקבצי הדוקר. מישהו כתב אותם ברשלנות, ויש בהם כמה באגים.

המשימה שלכם, למצוא את הבאגים, לתקן, ולהצליח להריץ ולהעלות את הפרויקט.

---

## מה צריך להשיג

### חלק א, לבנות אימג'

צריך להצליח לבנות אימג' מה-Dockerfile.

```bash
docker build -t docker-challenge .
```

רמז, הבנייה תיכשל בהתחלה. תקראו את הודעת השגיאה, היא מצביעה על השורה הבעייתית.

### חלק ב, להריץ קונטיינר

אחרי שהבנייה מצליחה, צריך להריץ קונטיינר, ולראות את האפליקציה עונה בדפדפן.

```bash
docker run -p 8080:3000 docker-challenge
```

הכתובת לבדיקה:

```text
http://localhost:8080
```

רמז, יכול להיות שהקונטיינר יקרוס מיד עם ההפעלה. תבדקו את הלוגים, ותראו איזה קובץ הוא מנסה להריץ.

### חלק ג, לוודא שהסודות לא דלפו

יש בפרויקט קובץ סודות. ודאו שהוא לא נכנס לתוך האימג'.

רמז, יש קובץ אחד שאחראי להחליט מה לא נכנס לאימג'. תבדקו מה כתוב בו.

### חלק ד, להעלות לדוקר האב

אחרי שהכל עובד, העלו את האימג' לחשבון שלכם בדוקר האב.

```bash
docker login
docker tag docker-challenge <שם-המשתמש-שלכם>/docker-challenge
docker push <שם-המשתמש-שלכם>/docker-challenge
```

---

## חלק בונוס, להרים גם מסד נתונים

יש בפרויקט קובץ שמרים אפליקציה ומסד נתונים יחד.

```bash
docker compose up --build
```

גם פה יש באגים. רמזים.

ראשון, הקובץ לא תקין מבחינת מבנה. תקראו את הודעת השגיאה על המבנה.

שני, גם אחרי שזה עולה, האפליקציה לא מתחברת למסד. תחשבו, כשהאפליקציה רצה בתוך קונטיינר, האם הכתובת של מסד הנתונים היא המחשב המקומי, או שם של שירות אחר.

הכתובת לבדיקה אחרי שהכל עובד:

```text
http://localhost:8080/api/users
```

---

## טיפים כלליים

תמיד תקראו את הודעת השגיאה לפני שאתם מנחשים. היא כמעט תמיד אומרת מה הבעיה.

תתקנו באג אחד בכל פעם, ותנסו שוב. ככה תדעו בדיוק מה תיקנתם.

בהצלחה.
